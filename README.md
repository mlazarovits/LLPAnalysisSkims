python dependencies for `macros/cmsCatFormat.py`
- mplhep (obvi)
- uproot (for file processing)
- hist (for histogram manipulation)
- matplotlib and numpy (also obviously)

these can all be installed in a conda environment. for mplhep, uproot, and hist you may need to install like `conda install -c conda-forge [package-name]`
